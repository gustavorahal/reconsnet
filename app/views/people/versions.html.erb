<%= render 'common/back_link', destino: @person.name, url: person_path(params.except(:id)) %>
<%= render 'common/page_title', titulo: "Alterações em <em>#{@person.name}</em>".html_safe %>

<%= render 'versions/versions', short: true %>
<%= paginate @versions %>