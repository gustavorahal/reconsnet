
<%= render partial: 'common/page_title', locals: { titulo: 'Editar conta' } %>

<%= bootstrap_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put },
                       layout: :horizontal, label_col: 'col-sm-2', control_col: 'col-sm-4') do |f| %>

    <%= devise_error_messages! %>

    <%= f.static_control :name, label: 'Nome' %>

    <%= f.email_field :email, autocomplete: 'off' %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <%= f.password_field :password, label: 'Nova senha', control_col: 'col-sm-3',
                         help: 'deixa em branco se você não quiser mudar sua senha',
                         autocomplete: 'off' %>
    <%= f.password_field :password_confirmation, label: 'Confirmar nova senha', control_col: 'col-sm-3',
                         autocomplete: 'off' %>

    <%= f.password_field :current_password, label: 'Senha atual', autocomplete: 'off', control_col: 'col-sm-3',
                         help: 'precisamos de sua senha atual para confirmar as mudanças' %>

    <%= f.form_group do %>
        <%= f.submit 'Salvar', class: 'btn btn-success' %>
        <%= link_to 'Cancelar', :back, class: 'btn btn-link' %>
    <% end %>

<% end %>

<br/>
<h3>Cancelar conta</h3>

Se por qualquer razão deseja cancelar sua conta clique no botão a seguir.
<p><%= button_to 'Cancelar minha conta', registration_path(resource_name),
                 class: 'btn btn-danger',
                 data: { confirm: 'Você tem certeza que deseja cancelar sua conta?' },
                 method: :delete %></p>
