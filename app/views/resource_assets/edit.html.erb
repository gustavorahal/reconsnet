<% title_frag = params[:asset_type].present? ? I18n.t("asset_types.#{params[:asset_type]}"): 'anexo' %>
<%= render 'common/page_title', titulo: "Editar <em>#{title_frag}</em>".html_safe  %>

<%= render 'form' %>
