

<%= render partial: 'common/page_title', locals:
        { titulo: "E-mails dos participantes do evento <em>#{@event.name}</em>".html_safe,
          back_url: event_path(@event)} %>

<% @participations.each do |p| %>
    <%= p.person.email %>
<% end %>