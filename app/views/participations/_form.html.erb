<%= bootstrap_form_for [@event, @participation],
                       layout: :horizontal,
                       label_col: 'col-sm-2', control_col: 'col-sm-6' do |f| %>

    <%= f.alert_message 'Favor corrigir o(s) erro(s) abaixo.' %>

    <%= hidden_field_tag :event_id, @event.id %>

    <%= f.static_control nil, label: 'Evento' do %>
        <%= @event.name %>
    <% end %>

    <% if action_name == 'new' %>
        <%= f.collection_select :person, @people, :id, :name, label: 'Pessoa', control_col: 'col-sm-3' %>
    <% else %> <!--edit -->
        <%= hidden_field_tag :person_id, @participation.person.id %>
        <%= f.static_control nil, label: 'Pessoa' do %>
            <%= @participation.person.name %>
        <% end %>
    <% end %>

    <%= f.select :status, Participation::STATUS, control_col: 'col-sm-3' %>
    <%= f.select :participation_type, Participation::TYPES, label: 'Tipo de participação ', control_col: 'col-sm-3' %>

    <%= f.form_group do %>
        <%= f.submit 'Salvar' %>
        <%= link_to 'Cancelar', event_path(@event), class: 'btn btn-link' %>
    <% end %>

<% end %>

