
<% flash2type = { alert: 'danger', notice: 'success' } %>

<% flash.map do |name, msg| %>
    <div class="alert alert-<%= flash2type[name] %> alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= msg %>
    </div>
<% end %>