
<%= render partial: 'common/titulo_pagina', locals: { titulo: 'Editar usuário' } %>


<%= bootstrap_form_for @usuario, layout: :horizontal, label_col: 'col-sm-2', control_col: 'col-sm-6' do |f| %>

    <%= f.alert_message 'Favor corrigir o(s) erro(s) abaixo.' %>

    <%= f.static_control nil, label: 'Evento' do %>
        <%= @usuario.name %>
    <% end %>

    <%= f.collection_select :papel, Usuario::PAPEIS, :to_s, :humanize, control_col: 'col-sm-2' %>

    <%= f.submit 'Salvar' %>

    <%= link_to 'Cancelar', usuarios_path, class: 'btn btn-link' %>

<% end %>
